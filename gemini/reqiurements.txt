opencv-python
Pillow
mss
google-genai

#PyAudio
# ---------- Option B: Homebrew + pip (if you prefer pip) ----------
# 1) Install PortAudio system library
# If Homebrew not installed: install it first (uncomment if needed)
# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# brew install portaudio

# 2) If pip still can't find headers, point pip to brew's portaudio path (common on M1/arm64)
# export PAAUDIO_PREFIX="$(brew --prefix portaudio)"
# export CPATH="${PAAUDIO_PREFIX}/include:${CPATH}"
# export LDFLAGS="-L${PAAUDIO_PREFIX}/lib ${LDFLAGS}"
# 3) Install PyAudio via pip
# python -m pip install --no-cache-dir PyAudio

# conda install -c conda-forge pyaudio -y


